{
  "name": "@op1/opencode-anthropic-auth",
  "version": "0.0.0-beta-18743",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/opzero1/opencode-anthropic-auth.git"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": {
    "bun": "1.3.14"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "tsc -p tsconfig.build.json",
    "build": "tsc -p tsconfig.build.json",
    "dev": "bun scripts/dev.ts",
    "dev:clean": "bun scripts/dev-clean.ts",
    "extract": "bun scripts/extract-system-prompt.ts",
    "test": "bun test",
    "types": "tsc",
    "format": "biome check --write --unsafe",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "release": "bun test && bun run types && bun run lint && bun run format:check && bun run build && npm publish"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "0.0.0-beta-18743"
  },
  "dependencies": {
    "effect": "4.0.0-rc.112"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@opencode-ai/plugin": "0.0.0-beta-18743",
    "@tsconfig/bun": "1.0.10",
    "@types/bun": "1.3.14",
    "lefthook": "2.1.9",
    "typescript": "6.0.3"
  }
}
